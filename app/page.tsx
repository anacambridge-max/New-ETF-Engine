"use client";
import {useEffect,useMemo,useState} from "react";
type Candle=[string,number,number,number,number,number,number];type ETF={symbol:string;name:string;exposure:string;role:string;price?:number;dayChangePct?:number;return5dPct?:number;return20dPct?:number;return90dPct?:number;high52Week?:number;drawdownPct?:number;volatility20dPct?:number;score?:number;signal:"ACCUMULATE"|"HOLD"|"WATCH"|"DATA ERROR";reason?:string;error?:string;candles?:Candle[]};type R={status:string;asOf?:string;marketRegime?:string;bestOpportunity?:string|null;data?:ETF[];error?:string};const pct=(v?:number)=>v===undefined?"—":`${v>0?"+":""}${v.toFixed(2)}%`;const money=(v?:number)=>v===undefined?"—":`₹${v.toFixed(2)}`;const cls=(v?:number)=>v===undefined||v===0?"neutral":v>0?"positive":"negative";const scls=(s:string)=>s==="ACCUMULATE"?"accumulate":s==="HOLD"?"hold":s==="WATCH"?"watch":"error";
function Chart({candles}:{candles?:Candle[]}){const p=(candles??[]).slice(-66).map(c=>c[4]);if(p.length<2)return <div className="chart-empty">Chart unavailable</div>;const min=Math.min(...p),span=Math.max(...p)-min||1;const points=p.map((v,i)=>`${i/(p.length-1)*100},${92-(v-min)/span*78}`).join(" ");return <svg viewBox="0 0 100 100" preserveAspectRatio="none" className="chart"><polyline points={points} fill="none" stroke="currentColor" strokeWidth="1.6" vectorEffect="non-scaling-stroke"/></svg>}
function Detail({etf,close}:{etf:ETF;close:()=>void}){return <div className="overlay" onClick={close}><aside className="drawer" onClick={e=>e.stopPropagation()}><div className="drawer-head"><div><span className="label">ETF DETAIL</span><h2>{etf.symbol}</h2><p>{etf.name} · {etf.role}</p></div><button className="close" onClick={close}>×</button></div><div className="score"><div><span className="label">OPPORTUNITY SCORE</span><strong>{etf.score??"—"}</strong></div><span className={`badge ${scls(etf.signal)}`}>{etf.signal}</span></div><div className="metrics">{[["Price",money(etf.price),""],["Today",pct(etf.dayChangePct),cls(etf.dayChangePct)],["5D",pct(etf.return5dPct),cls(etf.return5dPct)],["20D",pct(etf.return20dPct),cls(etf.return20dPct)],["90D",pct(etf.return90dPct),cls(etf.return90dPct)],["52W High",money(etf.high52Week),""],["Drawdown",pct(etf.drawdownPct),cls(etf.drawdownPct)],["20D Volatility",pct(etf.volatility20dPct),"neutral"]].map(([k,v,c])=><div className="metric" key={k}><span>{k}</span><strong className={c}>{v}</strong></div>)}</div><div className="chart-card"><div className="label">90-day price history</div><Chart candles={etf.candles}/></div><div className="why"><span className="label">WHY THIS SIGNAL</span><p>{etf.reason??etf.error??"No explanation available."}</p></div></aside></div>}
export default function Home(){const[data,setData]=useState<R|null>(null),[loading,setLoading]=useState(true),[selected,setSelected]=useState<ETF|null>(null);async function load(){setLoading(true);try{const r=await fetch("/api/market-data",{cache:"no-store"});setData(await r.json())}catch(e){setData({status:"error",error:e instanceof Error?e.message:"Unable to reach market-data engine",data:[]})}finally{setLoading(false)}}useEffect(()=>{void load();const id=setInterval(()=>void load(),300000);return()=>clearInterval(id)},[]);const rows=useMemo(()=>data?.data??[],[data]);const best=rows.find(x=>x.symbol===data?.bestOpportunity)??rows[0];const valid=rows.filter(x=>x.price!==undefined).length;const updated=data?.asOf?new Date(data.asOf).toLocaleString("en-IN",{day:"2-digit",month:"short",year:"numeric",hour:"2-digit",minute:"2-digit",hour12:false,timeZone:"Asia/Kolkata"}):"—";return <main className="dashboard"><header className="header"><div><div className="eyebrow">ADAPTIVE LONG-TERM INVESTING</div><h1>New ETF Engine</h1><p className="subtitle">10-ETF opportunity ranking powered by live Upstox market data</p></div><div className="live"><span className="dot"/>{loading?"Connecting…":valid===10?"Live Data Connected":`${valid}/10 Data Connected`}<small>Updated {updated} IST</small></div></header><section className="hero-grid"><div className="card best"><div className="label">TODAY'S BEST OPPORTUNITY</div><div className="best-row"><div><h2>{best?.symbol??(loading?"Loading…":"Unavailable")}</h2>{best?.price!==undefined&&<div className="price-line">{money(best.price)} <span className={cls(best.dayChangePct)}>{pct(best.dayChangePct)}</span></div>}</div>{best?.signal&&<span className={`badge ${scls(best.signal)}`}>{best.signal}</span>}</div><p>{best?.reason??data?.error??"Waiting for live market data."}</p></div><div className="card"><div className="label">MARKET REGIME</div><div className="big">{data?.marketRegime??"—"}</div><p>Derived from 90-day trend and breadth across the full ETF universe.</p></div><div className="card"><div className="label">ETF UNIVERSE</div><div className="big">10 ETFs</div><p>Core, commodities, growth, financials, technology and defensive exposure.</p></div></section><section className="card table-card"><div className="section-head"><div><h3>Opportunity Ranking</h3><p>Highest score first. Rows are clickable for detailed metrics.</p></div><button className="refresh" disabled={loading} onClick={()=>void load()}>{loading?"Refreshing…":"↻ Refresh"}</button></div><div className="table-wrap"><table><thead><tr><th>Rank</th><th>ETF</th><th>Exposure</th><th>Price</th><th>Today</th><th>5D</th><th>20D</th><th>90D</th><th>52W High</th><th>Drawdown</th><th>Score</th><th>Signal</th></tr></thead><tbody>{rows.length?rows.map((e,i)=><tr key={e.symbol} onClick={()=>setSelected(e)}><td data-label="Rank">{i+1}</td><td data-label="ETF"><strong>{e.symbol}</strong><span>{e.role}</span></td><td data-label="Exposure">{e.exposure}</td><td data-label="Price">{money(e.price)}</td><td data-label="Today" className={cls(e.dayChangePct)}>{pct(e.dayChangePct)}</td><td data-label="5D" className={cls(e.return5dPct)}>{pct(e.return5dPct)}</td><td data-label="20D" className={cls(e.return20dPct)}>{pct(e.return20dPct)}</td><td data-label="90D" className={cls(e.return90dPct)}>{pct(e.return90dPct)}</td><td data-label="52W High">{money(e.high52Week)}</td><td data-label="Drawdown" className={cls(e.drawdownPct)}>{pct(e.drawdownPct)}</td><td data-label="Score"><strong>{e.score??"—"}</strong></td><td data-label="Signal"><span className={`badge ${scls(e.signal)}`}>{e.signal}</span></td></tr>):<tr><td colSpan={12} className="empty">{loading?"Connecting to Upstox…":data?.error??"No market data returned."}</td></tr>}</tbody></table></div></section><footer>Research and decision-support only. Signals are not guarantees of future returns or personalized financial advice. The Upstox token is server-side only.</footer>{selected&&<Detail etf={selected} close={()=>setSelected(null)}/>}</main>}
